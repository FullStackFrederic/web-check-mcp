version: "3.9"
services:
  web-check:
    container_name: Web-Check
    build: .
    image: web-check-mcp:latest
    ports:
      - "${HOST_PORT:-3001}:3000"
    restart: unless-stopped
    environment:
      - ENABLE_MCP=true
